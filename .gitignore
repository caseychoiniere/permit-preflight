node_modules/
.next/
.env
.env.*
!.env.example
.DS_Store
coverage/
dist/
.vercel/
*.log
.idea/
playwright-report/
test-results/
/.swc

# scripts/package-chatgpt-context.sh always writes outside the repo (../permit-preflight-chatgpt-
# context.zip), so this can't normally be committed - kept as defense-in-depth in case that ever
# changes, and to ignore a stray copy someone drops back into the tree.
permit-preflight-chatgpt-context.zip
.vercel
.env*.local
