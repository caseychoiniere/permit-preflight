{
  "mcpServers": {
    "aidlc-reviewer": {
      "command": "npx",
      "args": ["tsx", "tools/aidlc-reviewer/src/index.ts"],
      "env": {
        "OPENAI_API_KEY": "${OPENAI_API_KEY}"
      }
    }
  }
}
